<form method="post" action="{{ path('app_abonnement_delete', {'idabonement': abonnement.idabonement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonnement.idabonement) }}">
    <button class="btn">Delete</button>
</form>

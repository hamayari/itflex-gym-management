{% extends "frontend/base.html.twig" %}

{% block title %}Modifier la réservation - FitPro Manager{% endblock %}
{% block headerCard %}<h3 class="text-center font-weight-light my-4"><i class="fas fa-edit me-2"></i>Modifier la réservation</h3>{% endblock %}

{% block container %}
    <div class="card shadow-sm">
        <div class="card-header bg-warning text-white">
            <i class="fas fa-calendar-check me-2"></i>Formulaire de modification
        </div>
        <div class="card-body">
            {{ include('participation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="mt-3 d-flex justify-content-between">
        <a href="{{ path('app_participation_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-2"></i>Retour à la liste
        </a>
        {{ include('participation/_delete_form.html.twig') }}
    </div>
{% endblock %}

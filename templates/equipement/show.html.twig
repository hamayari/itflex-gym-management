{% extends 'base_back_said.html.twig' %}



{% block title %}<center>Details Equipement</center>{% endblock %}

{% block body %}<center>
    <h1>Details Equipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEquipement</th>
                <td>{{ equipement.IdEquipement }}</td>
            </tr>
            <tr>
                <th>NomEquipement</th>
                <td>{{ equipement.NomEquipement }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ equipement.Quantite }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ equipement.DateAchat ? equipement.DateAchat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PrixAchat</th>
                <td>{{ equipement.PrixAchat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipement_index') }}">back to list</a>

    <a href="{{ path('app_equipement_edit', {'IdEquipement': equipement.IdEquipement}) }}">edit</a>

    {{ include('equipement/_delete_form.html.twig') }}
    </center>
{% endblock %}

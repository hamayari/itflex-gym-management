<form method="post" action="{{ path('app_offer_delete', {'idoffer': offer.idoffer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.idoffer) }}">
    <button class="btn">Delete</button>
</form>

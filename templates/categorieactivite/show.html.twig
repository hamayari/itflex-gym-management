{% extends 'base.html.twig' %}

{% block title %}Categorieactivite{% endblock %}

{% block body %}
    <h1>Categorieactivite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Categorie</th>
                <td>{{ categorieactivite.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorieactivite_index') }}">back to list</a>

    <a href="{{ path('app_categorieactivite_edit', {'id': categorieactivite.id}) }}">edit</a>

    {{ include('categorieactivite/_delete_form.html.twig') }}
{% endblock %}

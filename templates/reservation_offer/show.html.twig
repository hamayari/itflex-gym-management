{% extends "frontend/base.html.twig" %}

{% block title %}
	 ReservationOffer
{% endblock %}
{% block headerCard %}
	<h3 class="text-center font-weight-light my-4">ReservationOffer</h3>
{% endblock %}
{% block container %}

    <table class="table">
        <tbody>
            <tr>
                <th>Idreservation</th>
                <td>{{ reservation_offer.idreservation }}</td>
            </tr>
            <tr>
                <th>Datereservation</th>
                <td>{{ reservation_offer.datereservation ? reservation_offer.datereservation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Codepromo</th>
                <td>{{ reservation_offer.codepromo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_offer_index') }}">back to list</a>

    <a href="{{ path('app_reservation_offer_edit', {'idreservation': reservation_offer.idreservation}) }}">edit</a>

    {{ include('reservation_offer/_delete_form.html.twig') }}
{% endblock %}

{% extends 'backend/base.html.twig' %}

{% block title %}Modifier l'offre{% endblock %}
{% block page_title %}Gestion des Offres{% endblock %}
{% block breadcrumb %}
    <a href="{{ path('app_offer_index') }}" class="breadcrumb-item">Offres</a>
    <li class="breadcrumb-item active">Modifier</li>
{% endblock %}
 {% block nom %}
 
                         <h1 class="mt-4">Offer</h1>
                        <ol class="breadcrumb mb-4">
                            <li class="breadcrumb-item active">Edit Offer</li>
                        </ol>

                        
                        {% endblock %}
{% block body %}
   

    {{ include('offer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_offer_index') }}">back to list</a>

    {{ include('offer/_delete_form.html.twig') }}
    <div> <h1></h1>
</div>
{% endblock %}

{% block viewDetails%}

                            {% if app.request.attributes.get('_route') != 'app_offer_edit' %}
                        {% endif %}
                        {% endblock %}

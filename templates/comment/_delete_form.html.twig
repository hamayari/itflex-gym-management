<form method="post" action="{{ path('app_comment_delete', {'idc': comment.idc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.idc) }}">
   <button class="btn btn-danger">Delete</button>
</form>

{% extends "frontend/base.html.twig" %}

{% block title %}Modifier l'abonnement - FitPro Manager{% endblock %}
{% block headerCard %}<h3 class="text-center font-weight-light my-4"><i class="fas fa-edit me-2"></i>Modifier l'abonnement</h3>{% endblock %}

{% block container %}
    <div class="card shadow-sm">
        <div class="card-body">
            {{ include('abonnement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="mt-3 d-flex justify-content-between">
        <a href="{{ path('app_abonnement_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-2"></i>Retour à la liste
        </a>
        {{ include('abonnement/_delete_form.html.twig') }}
    </div>
{% endblock %}

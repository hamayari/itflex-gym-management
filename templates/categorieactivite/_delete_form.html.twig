<form method="post" action="{{ path('app_categorieactivite_delete', {'id': categorieactivite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorieactivite.id) }}">
    <button class="btn btn-danger btn-sm" onclick="confirmDelete('{{ path('app_categorieactivite_delete', {'id': categorieactivite.id}) }}', '{{ csrf_token('delete' ~ categorieactivite.id) }}')">
        <i class="fas fa-trash-alt"></i>supprimer
    </button>
</form>
